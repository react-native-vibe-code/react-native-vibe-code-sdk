E2B_API_KEY=
E2B_SANDBOX_TIMEOUT_MS= # 1 hour in milliseconds

ANTHROPIC_API_KEY=
# Disable API key and base URL input in the chat
# NEXT_PUBLIC_NO_API_KEY_INPUT=
# NEXT_PUBLIC_NO_BASE_URL_INPUT=

# Hide local models from the list of available models
# NEXT_PUBLIC_HIDE_LOCAL_MODELS=

NEXT_PUBLIC_BASE_URL=
NEXT_PUBLIC_APP_URL=
DATABASE_URL=
BETTER_AUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

NEXT_PUBLIC_PUSHER_APP_KEY=
PUSHER_APP_ID=
PUSHER_APP_SECRET=
NEXT_PUBLIC_PUSHER_CLUSTER=

TEMPLATE_SELECTION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=

GITHUB_TOKEN=
GITHUB_OWNER=

DEEPGRAM_API_KEY=
DEEPGRAM_ENV=
NEXT_PUBLIC_DEEPGRAM_API_KEY=

NEXT_PUBLIC_VOICE_ENABLED_FOR_MOBILE= # Voice Recording Settings
NEXT_PUBLIC_POLAR_START_PRODUCT_ID=
NEXT_PUBLIC_POLAR_PRO_PRODUCT_ID=
NEXT_PUBLIC_POLAR_SENIOR_PRODUCT_ID=
NEXT_PUBLIC_ENABLE_POSTHOG=
POLAR_ACCESS_TOKEN=
POLAR_WEBHOOK_SECRET=
POLAR_WEBHOOK_SECRET_DEV=


SANDBOX_PROVIDER=
DAYTONA_API_KEY=
DAYTONA_API_URL=

# ---------------------------------------------------------------------------
# AI Provider Mode
# ---------------------------------------------------------------------------
# Controls how Claude code generation runs. Two modes are available:
#
#   sandboxed  (default) — Runs inside an E2B cloud sandbox.
#                          Requires ANTHROPIC_API_KEY and E2B_API_KEY.
#                          Best for cloud/production deployments.
#
#   local-cli            — Spawns the locally-installed `claude` CLI as a
#                          subprocess using `claude -p "..." --output-format stream-json`.
#                          No E2B sandbox or ANTHROPIC_API_KEY required.
#                          Requires the Claude CLI to be installed and authenticated
#                          (`claude login`). Best for local development.
#
# When CLAUDE_PROVIDER=local-cli, projects live at LOCAL_PROJECTS_PATH/<projectId>
# and the app dev server is expected at http://localhost:8081.
#
CLAUDE_PROVIDER=sandboxed

# Root directory for local-cli project working directories.
# Each project gets its own subdirectory: $LOCAL_PROJECTS_PATH/<projectId>
# Defaults to ~/local-projects when not set.
LOCAL_PROJECTS_PATH=

### Optional env vars

# Domain of the site
NEXT_PUBLIC_SITE_URL=

# Rate limit
RATE_LIMIT_MAX_REQUESTS=
RATE_LIMIT_WINDOW=

# Vercel/Upstash KV (short URLs, rate limiting)
KV_REST_API_URL=
KV_REST_API_TOKEN=

NEXT_PUBLIC_PROD_URL=
OPENAI_API_KEY=

# Local testing configuration
# Set to 'true' to enable local deployment testing mode
LOCAL_DEPLOYMENT_MODE=

# Local Expo app directory
LOCAL_EXPO_APP_PATH=

# Local project ID for testing (you can change this)
LOCAL_PROJECT_ID=
EXPO_TOKEN=
POLAR_SERVER=
NEXT_PUBLIC_USE_BACKGROUND_CHAT=
INNGEST_BASE_URL=
NEXT_PUBLIC_REALTIME_VOICE=
CF_ACCOUNT_ID=
CF_API_TOKEN=
CF_DNS_API_TOKEN=
CF_DNS_ZONE_ID=

# Optional: Disable sandbox and Expo server health checks on window refocus
# Set to "true" to disable the automatic health check when user refocuses the browser tab
# This can be useful for debugging or when you don't want automatic restarts
NEXT_PUBLIC_DISABLE_REFOCUS_CHECK=
BLOB_READ_WRITE_TOKEN=
EXPO_PUBLIC_API_BASE_URL=
# Expo Go deep link URL (for OAuth callback)
CHAT_HISTORY_LIMIT=
EXPO_DEV_URL=
NGROK_AUTHTOKEN=
# Additional trusted origins for auth (comma-separated)
ADDITIONAL_TRUSTED_ORIGINS=
# Test user ID for development test mode
TEST_USER_ID=
# X Bot internal secret
X_BOT_SECRET=

CONVEX_OAUTH_CLIENT_ID=
CONVEX_OAUTH_CLIENT_SECRET=
PROVISION_HOST=
CONVEX_TEAM_SCOPED_TOKEN=
CONVEX_TEAM_SLUG=

NEXT_PUBLIC_CONVEX_URL=
TWITTER_CLIENT_SECRET=
TWITTER_CLIENT_ID=
TWITTER_REFRESH_TOKEN=
CONVEX_ADMIN_KEY=
FLY_API_TOKEN=
FLY_ORG_SLUG=
FLAG_STATIC_BUILD=

# Resend (email)
RESEND_API_KEY=
SERP_API=
EXA_API_KEY=
